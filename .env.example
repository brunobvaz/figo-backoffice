# Desenvolvimento: pedidos pelo proxy do Vite, no mesmo domínio do frontend.
VITE_API_URL=/api/v1
API_PROXY_TARGET=http://localhost:3000

# No Cloudflare, o build usa sempre /api/v1.
# Configura o destino do Worker em BACKEND_ORIGIN no wrangler.jsonc.
