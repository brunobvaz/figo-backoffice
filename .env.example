# Browser requests use the local proxy by default (recommended in production too).
VITE_API_URL=/api/v1
# Development proxy only. May point to a deployed API after admin routes are deployed.
API_PROXY_TARGET=http://localhost:3000
